package com.todolist.app;

import android.app.Application;

public class TodoApplication extends Application {
    @Override
    public void onCreate() {
        super.onCreate();
        // Khởi tạo các cấu hình ban đầu ở đây (ví dụ: Database, Notification Channel)
    }
}